import { ParseIntDefaultPipe } from './parse-int-default.pipe';

describe('ParseIntDefaultPipe', () => {
  it('should be defined', () => {
    expect(new ParseIntDefaultPipe()).toBeDefined();
  });
});
